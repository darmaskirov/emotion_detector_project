
from setuptools import setup, find_packages

setup(
    name="emotion-app",
    version="0.1.0",
    description="Emotion detector (Watson-style) with Flask deployment",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.9",
)
